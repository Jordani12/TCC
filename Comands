Fluxo de trabalho correto usando GIT BASH:

cd (pasta pra armazenar)

# Clonar o repo (caso for a primeira vez no PC)
git clone https://github.com/Jordani12/TCC.git (no master)

# Sempre trabalhe na sua branch 
git checkout (nome da sua branch) 

# Antes de começar, atualize com a master
git pull origin main

# Faça suas alterações no Unity...
# Depois commit e push
git add .
git commit -m "Minhas alterações"
git push origin (nome da sua branch)

# se precisar...
# Com espaços no nome - OBRIGATÓRIO as aspas
git config --global user.name "Jordani12"

# Email - SEMPRE com aspas
git config --global user.email "ojordani59@gmail.com"
